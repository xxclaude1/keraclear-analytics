# No environment variables required!
# All data is stored in Netlify Blobs (built into Netlify).
# GeoIP is handled via Netlify's x-country and x-nf-client-connection-city headers.

# App URL (set to your Netlify domain)
VITE_APP_URL=https://keraclear-analytics.netlify.app
